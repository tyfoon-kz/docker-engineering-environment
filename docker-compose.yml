services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: ecommerce-phpstorm-interpreter-app
    working_dir: /var/www/html
    command: ["php", "-S", "0.0.0.0:8080", "-t", "public"]
    ports:
      - "8080:8080"
    volumes:
      - ./:/var/www/html
    environment:
      APP_ENV: local
      COMPOSER_ALLOW_SUPERUSER: "1"
