# Domain Plan

The project uses the following domain model:

- `ecommerce.localho.st` — storefront
- `api.ecommerce.localho.st` — API
- `admin.ecommerce.localho.st` — admin panel

In homework 12 only the storefront is published. The other domains are documented now and will be routed in the next homework branch.
