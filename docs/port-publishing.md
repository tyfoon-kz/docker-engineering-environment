# Port Publishing

The host reaches the application through:

```text
http://localhost:${WEB_PUBLISHED_PORT}
```

`mysql` and `redis` are internal services and do not publish host ports in this reference.
